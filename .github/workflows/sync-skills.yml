name: Sync Skills Daily

on:
  schedule:
    # 每天凌晨 2 点同步
    - cron: '0 2 * * *'
  workflow_dispatch:
    # 手动触发

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync from local
        run: |
          echo "Skills backup - $(date)"
          echo "Run this manually when you update skills locally"
